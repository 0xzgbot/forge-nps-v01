# ═══════════════════════════════════════════════════════════════════════════
# AI Inference Provider Configuration
# ═══════════════════════════════════════════════════════════════════════════
# Cinesmith supports any OpenAI-compatible API endpoint.
# Pick ONE primary provider and comment out the others.

# ── Option A: Nous Research Portal (RECOMMENDED) ──
# https://portal.nousresearch.com | Bearer token auth
NOUS_ENDPOINT=https://inference-api.nousresearch.com/v1/chat/completions
NOUS_API_KEY=your_nous_api_key_here

# ── Option B: Moonshot AI (Kimi) native API ──
# https://platform.moonshot.cn
# KIMI_ENDPOINT=https://api.moonshot.cn/v1/chat/completions
# KIMI_API_KEY=your_moonshot_api_key_here

# ── Option C: NVIDIA NIM ──
# https://build.nvidia.com
# NIM_ENDPOINT=https://integrate.api.nvidia.com/v1/chat/completions
# KIMI_API_KEY=your_nvapi_key_here

# ── Option D: OpenRouter (proxy to 200+ models) ──
# https://openrouter.ai
# OPENROUTER_ENDPOINT=https://openrouter.ai/api/v1/chat/completions
# OPENROUTER_API_KEY=your_openrouter_key_here

# ── Storyboard-only image APIs ──
# These are used only by Script -> Storyboard image rendering when selected.
# Main Images, Characters, Videos, and remediation stay on Spark/ComfyUI.
# OPENAI_API_KEY=your_openai_api_key_here
OPENAI_IMAGE_MODEL=gpt-image-2
# GEMINI_API_KEY=your_google_ai_studio_key_here
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
STORYBOARD_IMAGE_PROVIDER=spark:flux2_dev

# ── Option E: Custom OpenAI-compatible endpoint ──
# CINESMITH_PROFILE_PROVIDER=custom
# CINESMITH_PROFILE_MODEL=your-model-name
# CINESMITH_PROFILE_BASE_URL=http://localhost:8000/v1
# NIM_ENDPOINT=http://localhost:8000/v1/chat/completions
# KIMI_DIRECTOR_ENDPOINT_API1=http://localhost:8000/v1/chat/completions
# KIMI_DIRECTOR_ENDPOINT_ACTIVE=api1

# ═══════════════════════════════════════════════════════════════════════════
# Model Names
# ═══════════════════════════════════════════════════════════════════════════
# Moonshot Kimi models via Nous Research Portal (OpenRouter-style IDs):
#   - ~moonshotai/kimi-latest      (default, always up to date)
#   - ~moonshotai/kimi-k2.6        (highest quality)
#   - ~moonshotai/kimi-k2          (balanced)
#   - ~moonshotai/kimi-k2-thinking (reasoning mode)
DIRECTOR_MODEL=~moonshotai/kimi-latest
THINKING_MODEL=~moonshotai/kimi-latest
VISION_MODEL=~moonshotai/kimi-latest

# Legacy aliases (kept for backward compatibility):
KIMI_INSTRUCT_MODEL=~moonshotai/kimi-latest
KIMI_THINKING_MODEL=~moonshotai/kimi-latest
KIMI_VISUAL_MODEL=~moonshotai/kimi-latest

COMFYUI_PRIMARY=http://localhost:8188
COMFYUI_SECONDARY=http://localhost:8189
DASHBOARD_PORT=7000
MOCK_MODE=false

# ═══════════════════════════════════════════════════════════════════════════
# Portable media + Hermes isolation (do not use ~/.hermes)
# ═══════════════════════════════════════════════════════════════════════════
# Leave CINESMITH_MEDIA_ROOT empty to auto-pick:
#   1) sibling folder ../CINESMITH_MEDIA if it exists
#   2) <repo>/media otherwise
# CINESMITH_MEDIA_ROOT=/absolute/path/to/CINESMITH_MEDIA
#
# Cinesmith always forces HERMES_HOME to <repo>/hermes_home unless you set:
# CINESMITH_ALLOW_GLOBAL_HERMES=1
# Prefer launching with: bash scripts/launch_cinesmith.sh

# LM Studio local inference fallback
LMSTUDIO_HOST=http://localhost:1234
LMSTUDIO_CHAT_MODEL=qwen2.5-3b-instruct
LMSTUDIO_VISION_MODEL=qwen3.6-35b-a3b
LMSTUDIO_EMBED_MODEL=text-embedding-nomic-embed-text-v1.5
USE_LOCAL_MODELS=false
USE_LOCAL_DIRECTOR=false
