# Cinesmith — Neural Production Studio
# Core dependencies for hackathon judges

# Async HTTP client for ComfyUI cluster communication
httpx>=0.27.0
aiohttp>=3.9.0

# HTTP client for validation/audit tooling
requests>=2.31.0

# Environment configuration
python-dotenv>=1.0.0

# Data handling
numpy>=1.26.0

# YAML parsing (Nexus manifest parsers)
PyYAML>=6.0

# Schema validation
pydantic>=2.0.0

# Testing
pytest>=9.0.0
pytest-asyncio>=0.23.0

# Dashboard
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
flask>=3.0.0  # audit test server (dev harness)

# Nexus graph engine (dashboard MCP tooling)
networkx>=3.0

# Image metadata/thumbnail handling
Pillow>=10.0.0

# Optional: for local embedding fallback (LM Studio)
# No pip package needed — LM Studio exposes OpenAI-compatible API on localhost:1234

# Optional: for dashboard/UI rich console output
# rich>=13.0.0
